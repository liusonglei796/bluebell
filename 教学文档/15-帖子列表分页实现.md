# 第15章:帖子列表分页实现

> **本章导读**
>
> 帖子可以创建和查看详情了,但用户还需要一个**帖子列表页**来浏览所有帖子。
> **分页(Pagination)**是解决大数据量展示的经典方案。

---

## 1. 分页请求参数

我们通过 `request.PostListRequest` 接收分页和排序参数。

#### 文件: `internal/dto/request/post.go`

```go
type PostListRequest struct {
	Page        int64  `json:"page" form:"page"`
	Size        int64  `json:"size" form:"size"`
	Order       string `json:"order" form:"order"`
	CommunityID int64  `json:"community_id" form:"community_id"`
}
```

---

## 2. Service 层实现

Service 层负责协调数据查询和多表关联。

#### 文件: `internal/service/post/post_service.go`

```go
func (s *PostService) GetPostList(ctx context.Context, p *request.PostListRequest) (data []*response.PostDetailResponse, err error) {
	// 1. 从 Redis 获取按顺序排列的帖子 IDs
	ids, err := s.postCache.GetPostIDsInOrder(ctx, p.Order, p.Page, p.Size)
	if err != nil {
		return nil, errorx.ErrServerBusy
	}

	if len(ids) == 0 {
		return make([]*response.PostDetailResponse, 0), nil
	}

	// 2. 根据 IDs 去 MySQL 批量获取帖子详情 (使用 IN 语句，保留原有顺序)
	posts, err := s.postRepo.GetPostListByIDsWithPreload(ctx, ids)
	if err != nil {
		return nil, errorx.ErrServerBusy
	}

	// 3. 从 Redis 批量获取这些帖子的投票数
	voteData, err := s.voteCache.GetPostsVoteData(ctx, ids)
	if err != nil {
		return nil, errorx.ErrServerBusy
	}

	// 4. 拼装响应数据
	data = make([]*response.PostDetailResponse, 0, len(posts))
	for idx, post := range posts {
		authorName := "未知用户"
		if post.Author != nil {
			authorName = post.Author.UserName
		}

		postDetail := &response.PostDetailResponse{
			ID:          post.PostID,
			AuthorID:    strconv.FormatInt(post.AuthorID, 10),
			CommunityID: post.CommunityID,
			Status:      post.Status,
			Title:       post.PostTitle,
			Content:     post.Content,
			CreateTime:  post.CreatedAt,
			AuthorName:  authorName,
			VoteNum:     voteData[idx],
		}
		data = append(data, postDetail)
	}
	return data, nil
}
```

---

## 3. DAO 层实现

#### 文件: `internal/dao/mysql/post.go`

```go
// GetPostListByIDsWithPreload 根据给定的ID列表查询帖子详情（带预加载）
func (r *PostRepo) GetPostListByIDsWithPreload(ctx context.Context, ids []string) (posts []*model.Post, err error) {
	if len(ids) == 0 {
		return make([]*model.Post, 0), nil
	}

	posts = make([]*model.Post, 0, len(ids))
	err = r.db.WithContext(ctx).
		Preload("Author").
		Preload("Community").
		Where("post_id IN ?", ids).
		Where("status = ?", 1). // 过滤软删除
		Find(&posts).Error

	if err != nil {
		return nil, fmt.Errorf("query post list by ids failed: %w", err)
	}

	// 按照传入的 ids 顺序排列 MySQL 查出的结果
	postMap := make(map[string]*model.Post, len(posts))
	for _, post := range posts {
		postMap[post.PostID] = post
	}

	orderedPosts := make([]*model.Post, 0, len(ids))
	for _, id := range ids {
		if post, ok := postMap[id]; ok {
			orderedPosts = append(orderedPosts, post)
		}
	}

	return orderedPosts, nil
}
```

---

**下一章:** [第16章:帖子投票功能深度解析](./16-帖子投票功能深度解析.md)

**返回目录:** [README.md](./README.md)
